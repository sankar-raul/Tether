import styles from './session-settings.module.css'

const SessionSettings = () => {

    return (
        <section className={styles['session-settings']}>
            Session Settings
        </section>
    )
}
export default SessionSettings